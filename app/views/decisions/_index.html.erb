<div class="decisions-container">
  <% if current_tab == 'user' %>
    <%= render partial: 'user_decisions', locals: { pagination: pagination } %>
  <% else %>
    <%= render partial: 'comment_decisions', locals: { pagination: pagination } %>
  <% end %>
</div>

<%= render partial: 'shared/pagination', locals: { pagination: pagination, query_params: { target_type: params[:target_type] } } %>
