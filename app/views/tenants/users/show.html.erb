<div class="container">
  <div class="tenant-detail-header">
    <h1 class="text-title"><%= @membership.display_name %></h1>
    <div class="tenant-detail-identifier-wrapper">
      <span class="text-caption"><%= t('.user_id_label') %>: <%= @user.id %></span>
      <button class="icon-btn"
              data-controller="clipboard"
              data-clipboard-text-value="<%= @user.id %>"
              data-action="click->clipboard#copy">
        <span class="material-icons" data-clipboard-target="icon">content_copy</span>
        <span class="material-icons hidden" data-clipboard-target="successIcon">check</span>
      </button>
    </div>
  </div>

  <div class="divider--thick"></div>

  <div class="tenant-detail-section">
    <h2 class="text-heading"><%= t('.invitation_guide_title') %></h2>
    <div class="tenant-detail-description">
      <p class="text-body"><%= t('.invitation_guide_description') %></p>
    </div>
  </div>

  <div class="tenant-detail-back divider">
    <%= link_to "javascript:history.back()", class: "text-link" do %>
      ← <%= t('.back') %>
    <% end %>
  </div>
</div>
