---
ja:
  activerecord:
    attributes:
      tenant:
        active: 有効
        created_at: 作成日時
        description: 説明
        name: テナント名
        slug: 識別子
        updated_at: 更新日時
    errors:
      models:
        tenant:
          attributes:
            slug:
              invalid: は英小文字、数字、ハイフンのみ使用できます
    models:
      tenant: テナント
