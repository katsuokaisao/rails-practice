---
en:
  activerecord:
    attributes:
      user:
        nickname: Nickname
        time_zone: Time Zone
    errors:
      models:
        user:
          attributes:
            password:
              invalid: can only contain alphanumeric characters and symbols
            time_zone:
              inclusion: is not a valid time zone
